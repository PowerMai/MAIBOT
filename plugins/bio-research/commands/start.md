# start

Use this command to start a bio-research workflow from problem framing to execution setup.

## Instructions

1. Use `scientific-problem-selection` to scope objective and constraints.
2. Route to `nextflow-development`, `single-cell-rna-qc`, or `scvi-tools` as needed.
3. Return a phased plan with required datasets, tools, and checkpoints.
