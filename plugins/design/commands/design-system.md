# design-system

Use this command to evaluate or evolve the design system.

## Instructions

1. Use `design-system-management` to assess standards and component health.
2. Use `design-handoff` to define migration guidance for teams.
3. Return proposed changes, rollout sequence, and governance notes.
