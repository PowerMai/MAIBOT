# accessibility

Use this command to run an accessibility-focused design review.

## Instructions

1. Use `accessibility-review` to assess major user flows and components.
2. Use `design-handoff` to capture implementation requirements for fixes.
3. Return prioritized issues, severity, and recommended remediation.
