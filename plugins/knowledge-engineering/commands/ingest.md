# ingest

Use this command to ingest new knowledge sources into the target knowledge base.

## Instructions

1. Use `kb-user-ingest` and `kb-web-harvest` to collect structured and unstructured inputs.
2. Use `kb-entity-extract` and `kb-ontology-import` to normalize entities and relationships.
3. Return ingestion summary, validation status, and follow-up cleanup tasks.
