diff --git a/node_modules/@assistant-ui/react-langgraph/dist/convertLangChainMessages.js b/node_modules/@assistant-ui/react-langgraph/dist/convertLangChainMessages.js
index 0000000..1111111 100644
--- a/node_modules/@assistant-ui/react-langgraph/dist/convertLangChainMessages.js
+++ b/node_modules/@assistant-ui/react-langgraph/dist/convertLangChainMessages.js
@@ -60,6 +60,15 @@ const contentToParts = (content) => {
                     args: part.action,
                     argsText: JSON.stringify(part.action),
                 };
+            case "tool-call":
+                return {
+                    type: "tool-call",
+                    toolCallId: part.id ?? "",
+                    toolName: part.name ?? "",
+                    toolCall: { id: part.id ?? "", name: part.name ?? "" },
+                    args: part.args ?? {},
+                    argsText: JSON.stringify(part.args ?? {}),
+                    ...(part.result !== undefined && part.result !== null && { result: typeof part.result === "string" ? part.result : String(part.result) }),
+                };
             default:
                 const _exhaustiveCheck = type;
                 warnForUnknownMessagePartType(_exhaustiveCheck);
@@ -99,22 +108,10 @@ export const convertLangChainMessages = (message) => {
             const hasToolCallInContent = Array.isArray(normalizedContent) && normalizedContent.some((p) => p?.type === "tool-call");
             if (hasToolCallInContent && Array.isArray(normalizedContent)) {
-                const content = normalizedContent.map((part) => {
-                    if (part?.type === "text")
-                        return { type: "text", text: part.text ?? "" };
-                    if (part?.type === "tool-call") {
-                        const result = part.result;
-                        return {
-                            type: "tool-call",
-                            toolCallId: part.id ?? "",
-                            toolName: part.name ?? "",
-                            args: part.args ?? {},
-                            argsText: JSON.stringify(part.args ?? {}),
-                            ...(result !== undefined && result !== null && { result: typeof result === "string" ? result : String(result) }),
-                        };
-                    }
-                    return null;
-                }).filter((a) => a !== null);
+                const content = contentToParts(normalizedContent);
                 return {
                     role: "assistant",
                     id: message.id,
diff --git a/node_modules/@assistant-ui/react-langgraph/dist/appendLangChainChunk.js b/node_modules/@assistant-ui/react-langgraph/dist/appendLangChainChunk.js
index 0000000..1111111 100644
--- a/node_modules/@assistant-ui/react-langgraph/dist/appendLangChainChunk.js
+++ b/node_modules/@assistant-ui/react-langgraph/dist/appendLangChainChunk.js
@@ -27,27 +27,4 @@ function contentPartsToMerged(parts) {
         } else {
             merged.push(p);
         }
     }
-    // 全段合并：避免后端多次 flush 导致多个 text/reasoning 造成句子断裂或思考块割裂（最多各保留一段）
-    let allText = "";
-    let allReasoning = "";
-    const toolCalls = [];
-    let firstTextIdx = -1;
-    let firstReasoningIdx = -1;
-    for (let i = 0; i < merged.length; i++) {
-        const p = merged[i];
-        if (p.type === "text") {
-            allText += (p.text || "");
-            if (firstTextIdx < 0) firstTextIdx = i;
-        } else if (p.type === "reasoning") {
-            allReasoning += (allReasoning ? "\n\n" : "") + (p.text || "");
-            if (firstReasoningIdx < 0) firstReasoningIdx = i;
-        } else {
-            toolCalls.push(p);
-        }
-    }
-    const out = [];
-    const textFirst = firstReasoningIdx < 0 || (firstTextIdx >= 0 && firstTextIdx < firstReasoningIdx);
-    if (textFirst && allText) out.push({ type: "text", text: allText });
-    if (allReasoning) out.push({ type: "reasoning", text: allReasoning });
-    if (!textFirst && allText) out.push({ type: "text", text: allText });
-    for (const t of toolCalls) out.push(t);
-    return out.length ? out : merged;
+    return merged.length ? merged : undefined;
 }
 export const appendLangChainChunk = (prev, curr) => {
+@@ -35,6 +35,19 @@ export const appendLangChainChunk = (prev, curr) => {
+     if (!prev || prev.type !== "ai") {
+-        return {
+-            ...curr,
+-            type: curr.type.replace("MessageChunk", "").toLowerCase(),
+-        };
+        if (curr.content_parts && Array.isArray(curr.content_parts)) {
+            const mergedContent = contentPartsToMerged(curr.content_parts);
+            if (mergedContent && mergedContent.length > 0) {
+                return {
+                    ...curr,
+                    type: curr.type.replace("MessageChunk", "").toLowerCase(),
+                    content: mergedContent,
+                    content_parts: curr.content_parts,
+                };
+            }
+        }
+        return {
+            ...curr,
+            type: curr.type.replace("MessageChunk", "").toLowerCase(),
+        };
+    }
 @@ -60,22 +60,49 @@ export const appendLangChainChunk = (prev, curr) => {
         else {
             newContent.push({ type: "text", text: curr.content });
         }
     }
     else if (Array.isArray(curr.content)) {
-        const lastIndex = newContent.length - 1;
+        let lastIndex = newContent.length - 1;
         for (const item of curr.content) {
             if (!("type" in item)) {
                 continue;
             }
             if (item.type === "text") {
                 if (newContent[lastIndex]?.type === "text") {
                     newContent[lastIndex].text =
-                        newContent[lastIndex].text + item.text;
+                        (newContent[lastIndex].text || "") + (item.text || "");
                 }
                 else {
-                    newContent.push({ type: "text", text: item.text });
+                    newContent.push({ type: "text", text: item.text ?? "" });
                 }
             }
             else if (item.type === "image_url") {
                 newContent.push(item);
             }
+            else if (item.type === "reasoning") {
+                if (newContent[lastIndex]?.type === "reasoning") {
+                    newContent[lastIndex].text =
+                        (newContent[lastIndex].text || "") + "\n\n" + (item.text ?? "");
+                }
+                else {
+                    newContent.push({ type: "reasoning", text: item.text ?? "" });
+                }
+            }
+            else if (item.type === "tool-call") {
+                const part = {
+                    type: "tool-call",
+                    id: item.id ?? "",
+                    name: item.name ?? "",
+                    toolCall: { id: item.id ?? "", name: item.name ?? "" },
+                    args: item.args ?? {},
+                };
+                if (item.result !== undefined && item.result !== null)
+                    part.result = typeof item.result === "string" ? item.result : String(item.result);
+                newContent.push(part);
+            }
+            lastIndex = newContent.length - 1;
         }
     }
     const newToolCalls = [...(prev.tool_calls ?? [])];
