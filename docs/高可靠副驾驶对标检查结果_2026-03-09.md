# 高可靠副驾驶对标检查结果（2026-03-09）

基于「高可靠副驾驶 Agent OS — 对标 Cursor/Claude/Cowork 检查计划」执行，按「可发现性 / 一致性 / 可靠性 / 对齐 Cursor / 对齐 Claude-Cowork」五维记录结论与证据。

---

## 一、门禁与自动化结果

| 类别 | 结果 | 说明 |
|------|------|------|
| check:events | 通过 | 未发现事件硬编码 |
| check:session-state | 通过 | 会话/模式写入已收敛 |
| check:session-flow | 通过 | 会话插件隔离与 Plan 确认流转链路已就绪 |
| check:role-mode-contract | 通过 | 第四模式前后端契约已闭环 |
| check:slash-mode | 通过 | slash 模式透传与分支可达性已收敛 |
| check:task-entry | 通过 | 任务主入口判定在多视图保持一致 |
| test_done_verifier_*（6 个） | 通过 | Ask/Plan/Debug/Review/Agent/空结果 用例全部通过 |
| test_middleware_chain_consistency（2 个） | 通过 | ask 链与模式名一致 |
| test_reasoning_stream_contract（4 个） | 通过 | reasoning 事件形状与前端解析一致 |

---

## 二、五维检查结论

### 可发现性 — 通过

| 项 | 结论 | 证据 |
|----|------|------|
| 推荐流程与当前模式建议下一步 | 通过 | `modeFlowConstants.ts` 单源定义 RECOMMENDED_FLOW_SHORT、NEXT_STEP_BY_MODE；AgentCapabilities 展示「推荐流程」「建议下一步」；Composer 模式切换 toast 含「建议下一步」（cursor-style-composer.tsx L982-983） |

### 一致性 — 通过

| 项 | 结论 | 证据 |
|----|------|------|
| 完成语义单源 | 通过 | mode_config.py 定义 MODE_COMPLETION_MARKERS、MODE_COMPLETION_FAIL_SUGGESTION；done_verifier.py 仅引用、无本地重复 markers |
| 会话状态写入 | 通过 | checklist §2.6 会话状态写入契约已审计；门禁 check:session-state 通过 |
| Plan 确认双入口切 Agent | 通过 | InterruptDialog 与 PlanToolUI 均读 maibot_plan_confirm_switch_to_agent，批准后 setScopedChatMode('agent') + CHAT_MODE_CHANGED |

### 可靠性 — 通过

| 项 | 结论 | 证据 |
|----|------|------|
| DoneVerifier 与提示词对齐 | 通过 | 6 个用例覆盖各模式及空结果；markers 与 FOUR_MODES_DESIGN §六 描述一致 |
| HITL / interrupt 协议 | 通过 | behavior_analysis §2 §3 已落实；InterruptDialogGuard、resume + INTERRUPT_RESOLVED 接流 |

### 对齐 Cursor — 通过（1 项待产品定稿）

| 项 | 结论 | 证据 |
|----|------|------|
| Footer/Composer/消息区/工具卡/错误/会话/工作区/五模式与 Diff | 通过 | [cursor_alignment_checklist.md](cursor_alignment_checklist.md) §2.1–2.9 表中结论与当前实现一致 |
| 附件/上下文芯片 | 待产品定稿 | checklist §2.2 仍为「待产品以 Cursor 核对后定稿」；当前实现说明已写入该表 |

### 对齐 Claude/Cowork — 通过

| 项 | 结论 | 证据 |
|----|------|------|
| 极简工具+对话驱动、Skills/MCP、会话/角色绑定、协作与任务 | 通过 | [product_parity_claude_cursor_cowork.md](product_parity_claude_cursor_cowork.md)、[claude_cowork_parity_scorecard_2026-03-02.md](claude_cowork_parity_scorecard_2026-03-02.md)；剩余差距为命令即模式等价、编辑区回写 run 等低优先级 |

---

## 三、文档与代码一致性复核

- **FOUR_MODES_DESIGN.md §六 §七**：完成语义单源、Plan 确认双入口描述与 mode_config、InterruptDialog、ux_and_ops_notes §9 一致。
- **ux_and_ops_notes.md §9**：Plan 确认双入口与等价性、maibot_plan_confirm_switch_to_agent 两处读取与代码一致。
- **product_parity / behavior_analysis**：剩余差距与「差距与建议改法」仍准确；高可靠优化已落地项（推荐流程、完成语义、Plan 双入口）已在 product_parity §五 索引中通过本检查报告与 FOUR_MODES_DESIGN/ux_and_ops_notes 体现。

---

## 四、优先修复建议（合并入 Backlog）

与 [product_alignment_analysis_2026-03-04.md](product_alignment_analysis_2026-03-04.md) 及 parity 评分卡一致，本次检查未发现新的 P0 单源或协议违反；建议继续按既有 Backlog 推进：

1. **P1**：前端关键路径静默 catch 收敛（工作区/档案/模型加载失败可感知）— 见 product_alignment_analysis §3 P1、bugs_and_product_alignment_backlog §二。
2. **P1**：会话/工作区边界（profile 缓存时序、高线程量截断、工作区切换分叉）— 已记录，profile 一致性建议保留回归用例。
3. **P2**：UI 门禁发布前勾选、API 错误形状收敛、a11y/i18n 扫尾 — 每次发布例行检查。

若后续人工真机与 Cursor 对比或附件/上下文芯片产品定稿后，可更新 cursor_alignment_checklist §2.2 并刷新本报告。

---

## 五、进一步优化检查结果（2026-03-09）

在既有高可靠对标、P1 静默 catch 收敛与 code_audit 基础上，按「进一步优化检查与 Cursor 差距」计划执行的一轮复核与差距固化。

### 5.1 自动化与门禁结果

| 类别 | 结果 | 说明 |
|------|------|------|
| check:events | 通过 | 未发现事件硬编码 |
| check:session-state | 通过 | 会话/模式写入已收敛 |
| check:session-flow | 通过 | 会话插件隔离与 Plan 确认流转链路已就绪（门禁校验字面量已放宽为 `maibot_plan_confirmed_thread_`，与实现 validTid 一致） |
| check:role-mode-contract | 通过 | 第四模式前后端契约已闭环 |
| check:slash-mode | 通过 | slash 模式透传与分支可达性已收敛 |
| check:task-entry | 通过 | 任务主入口判定在多视图保持一致 |
| test_done_verifier_*（6 个） | 通过 | Ask/Plan/Debug/Review/Agent/空结果 用例全部通过 |
| test_middleware_chain_consistency（2 个） | 通过 | ask 链与模式名一致 |
| test_reasoning_stream_contract（4 个） | 通过 | reasoning 事件形状与前端解析一致 |

### 5.2 Checklist 逐区代码复核结论

按 [cursor_alignment_checklist.md](cursor_alignment_checklist.md) §2.1–2.9 逐区核对结论与当前代码：

- **§2.1**：showRunStrip 条件为 `!streamJustEnded && runtimeRunning && taskRunningFromEvent !== false && (hasSteps \|\| hasTodos \|\| runSummary.running)`，与表中「仅在有 steps 或 todos 时显示」及 drain 双路径约定一致。
- **§2.2**：附件/上下文芯片 — 当前实现：ContextItemChip、ComposerAttachments、ComposerAddAttachment（attachment.tsx）、一行多 chip、有编辑器选区时「已选中 N 行代码」（cursor-style-composer.tsx）。与 checklist 描述一致；**仍待产品以 Cursor 核对后定稿**。
- **§2.3–2.9**：mergeToolResultsIntoAiMessages（messages/complete 与 updates）、Plan 双入口（InterruptDialog + PlanToolUI 读 maibot_plan_confirm_switch_to_agent）、session_context 仅当 `currentThreadIdRef.current === threadId` 时写 setScopedChatMode/setScopedActiveRoleIdInStorage 等，与表中结论一致。

**结论**：§2.1–2.9 与实现一致；§2.2 仍待产品定稿。

### 5.3 与 Cursor 差距清单

便于产品与真机核对使用的汇总表：

| 类型 | 项 | 说明 | 建议 |
|------|-----|------|------|
| **待产品定稿** | 附件/上下文芯片 §2.2 | 当前：一行多 chip、可删、支持文件/路径/选区/代码片段；Cursor 可能为折叠「已选 N 个文件」或不同布局 | 产品以 Cursor 实际界面核对后，在 checklist 中定稿「一致」或写明确改法 |
| **待人工核对** | 编辑区接受/拒绝是否回写 run | behavior_analysis §1.3：当前编辑区仅影响缓冲区；若 Cursor 有回写 run，需后端协议+前端接流 | 真机核对后决定是否排期；若要对齐详见 [cursor_claude_cowork_behavior_analysis.md](cursor_claude_cowork_behavior_analysis.md) §1.3 扩展建议 |
| **待人工核对** | 先执行再确认 | behavior_analysis §2.3：当前仅「先暂停再执行」；若 Cursor 部分操作为「先执行再确认」，需扩展中间件与工具卡状态 | 真机核对后决定是否排期；若要对齐详见 [cursor_claude_cowork_behavior_analysis.md](cursor_claude_cowork_behavior_analysis.md) §2.3 |
| **已知差异（可接受）** | 4 种 vs 5 种模式 | Cursor 常见 Agent/Ask/Plan/Edit；本系统扩展 Debug、Review；行为上 Ask=只读、Plan→Agent 与 Cursor 一致 | 已在 checklist §2.9 注明，无需改 |
| **已知差异（可接受）** | 命令即模式完全等价 | mode_vs_command_parity：与 Claude「命令即模式」有实现差异，当前兼容并存 | 见 product_parity §四，按产品策略决定是否收敛 |

**若排期对齐 — 主要改动模块与依赖**（不估人天，供 Backlog 参考）：

- **附件/上下文芯片 §2.2**：若改为折叠或不同布局，主要动前端 Composer 区（cursor-style-composer.tsx、ContextItemChip、attachment.tsx）；依赖设计稿与 Cursor 真机结论。
- **编辑区接受/拒绝回写 run**：需后端新增「编辑区结果」事件或 API、前端 FullEditorV2Enhanced 接受/拒绝回调与 MyRuntimeProvider 接流更新工具卡；依赖 run 状态/stream 协议约定。
- **先执行再确认**：需后端 DiffAwareHumanInTheLoopMiddleware 与 core_tools 配置扩展、前端 WriteFileToolUI/EditFileToolUI 等工具卡状态「已执行·可回退」及回退请求；依赖产品确认 Cursor 具体场景。

索引：本清单在 [cursor_alignment_checklist.md](cursor_alignment_checklist.md) 中以「与 Cursor 剩余差距汇总」小节引用；[product_parity_claude_cursor_cowork.md](product_parity_claude_cursor_cowork.md) §四 已增加指向本清单的索引。

### 5.4 与 Cursor 真机核对检查表

产品/设计与 Cursor 真机对比时可按下表勾选并填写结论，便于收口与更新 5.3/checklist。

| 差距项 | 核对要点 | 结论（一致 / 不一致需改，由人工填写） |
|--------|----------|----------------------------------------|
| 附件/上下文芯片 §2.2 | 布局（多 chip 平铺 vs 折叠「已选 N 个文件」）；文案（如「已选中 N 行代码」）；删除方式（每项可删 vs 整组清空）；类型支持（文件/路径/选区/代码片段） |  |
| 编辑区接受/拒绝回写 run | 编辑区接受 diff 后 Cursor 是否更新 run/工具卡状态；拒绝后是否回写 run 或仅本地缓冲区 |  |
| 先执行再确认 | Cursor 是否存在「先执行再在工具卡上接受/拒绝」的操作类型；若有，具体是哪些工具/场景 |  |

**其他待人工核对项**（当前实现已固化，结论待真机核对后更新 behavior_analysis / checklist）：

- **工具完成时轻量 Toast**：当前仅 run 结束（stream_end）时 toast「本轮完成」/「共 N 个工具」；单次 tool_end 无轻量通知。若 Cursor 有单次工具完成的轻量 Toast，可按需在 thread 订阅 tool_end 时增加 toast。
- **接受/拒绝粒度**：当前编辑区与聊天区 diff 均为**整文件**接受/拒绝（FullEditorV2Enhanced 接受后 handleSaveFile 整文件写盘，拒绝恢复 diffOriginal 到缓冲区）。逐段/逐行粒度仍待人工核对，若需支持需扩展 diff 区块与写盘粒度。

**可选**：E2E 3.1.1（运行状态与步骤展示）、7.4（思考/流式/工具/UI）本次未执行，建议发布前按 [E2E_FUNCTIONAL_TEST_PLAN.md](E2E_FUNCTIONAL_TEST_PLAN.md) 补跑。
