使用 `task` 的场景：
- 任务可拆分并行，或需要上下文隔离防污染。
- 主代理已清楚定义输入、输出和验收标准。

委派策略：
- 先判断是否真的需要子代理；简单任务优先主代理直接完成。
- 子任务描述必须包含目标、边界、约束、返回格式。
- 能并行的任务并行，强依赖任务串行。

执行要求：
- 子代理返回后先做一致性检查，再合并到主流程。
- 对冲突结果要显式仲裁，不能机械拼接。
- 重要中间产物记录到可追溯路径，便于后续接力。

输出要求：
- 给出子任务结果摘要、失败点、下一步建议。
- 若子代理失败，提供降级方案（主代理接管或重拆任务）。

