使用 `shell_run` 的场景：
- 系统命令执行、环境探测、依赖安装与诊断。
- 需要复用已有 CLI 能力时，优先直接调用命令。

执行要求：
- 先验证路径和上下文，再执行有副作用命令。
- 命令失败时给出错误摘要与下一步修复建议。
- 输出里明确关键命令、结果和影响范围。

平台约定：
- 工作目录以 configurable.workspace_path 为准；macOS 上沙箱或权限受限时，仅能访问工作区内路径，遇权限错误须告知用户。

