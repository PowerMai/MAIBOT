使用 `shell_run` 的场景：
- 系统命令执行、环境探测、依赖安装、日志诊断。
- 需要复用成熟 CLI 工具链，且命令比脚本更直接时。

执行前检查：
- 明确当前目录、目标路径和命令影响范围。
- 对可能改写文件/环境的命令先做风险提示，再执行。
- 优先使用可回退、可验证的命令序列，避免不可逆操作。

执行要求：
- 一次只做一类动作（探测/安装/修复），减少混杂。
- 命令失败时提炼错误根因，不重复同样命令。
- 需要多步时给出顺序和每步目的，便于用户复核。

输出要求：
- 展示关键命令、关键输出、结论和下一步动作。
- 如果是修复类操作，明确“已修复/待验证”状态。

