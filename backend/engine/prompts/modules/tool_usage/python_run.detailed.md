使用 `python_run` 的场景：
- 数值计算、统计、矩阵评分、数据清洗与验证。
- 需要可复现结论时，优先写短脚本而非口算。
- 需要批量处理文件或做结构化数据转换时。

执行要求：
- 先说明输入数据来源和假设，再执行脚本。
- 脚本尽量短小、可读、可复现；避免一次性超长脚本。
- 关键结论必须包含可核验字段（参数、公式、样本量、路径）。
- 若结果异常，先打印中间变量并最小化重试，不盲目重复执行。

输出要求：
- 给出结果摘要 + 下一步建议。
- 涉及文件时使用绝对路径。
- 对高风险结论附“验证方法”或二次校验建议。

